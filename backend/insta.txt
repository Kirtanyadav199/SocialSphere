==> Authentication  
  => register ()
  => login
  => log out(token blacklisting)
  => [OTP based registration]

==> Post
   => create
   => can see the feed
   => like Posts(collection types)
   => save Posts

==> users
   => followers
   => following